package HomeWork5.test;

import org.junit.Assert;
import org.junit.Test;

import java.util.Scanner;

import static org.junit.Assert.*;

public class RegExSearchTest {

    private RegExSearchTest regExSearch = new RegExSearchTest();


    @Test
    public void search() {
        /*
        Scanner console =new Scanner(System.in);
        System.out.println("введите текст");
        String text = console.nextLine();
        System.out.println("введите слово");
        String word = console.nextLine();
        System.out.println("введите количество повторений, расчитанных самостоятельно");
        Long count =console.nextLong();

        Assert.assertEquals(count, regExSearch.search(text,word));

         */
        /*
        String text = "Мама мыла раму у своей мамы вместе с мама";
        long actual = regExSearch.search(text, "Мама");

        Assert.assertEquals(1,actual);

         */
    }
}